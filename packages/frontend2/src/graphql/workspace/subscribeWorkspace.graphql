subscription SubscribeWorkspace($workspaceId: ID!) {
  workspace(workspaceId: $workspaceId) {
    id
    name
    createdAt
    onboardingSteps {
      id
      stepId
      type
      status
      completedAt
      dismissedAt
      priority
      createdAt
    }
  }
}
