# Link MCP Tool to Runtime
# WHY: Connect a tool to a runtime (tool set) for management
mutation LinkMCPToolToRuntime($mcpToolId: ID!, $runtimeId: ID!) {
  linkMCPToolToRuntime(mcpToolId: $mcpToolId, runtimeId: $runtimeId) {
    id
    name
    mcpToolCapabilities {
      id
      name
    }
  }
}
