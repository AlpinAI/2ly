# Subscribe to runtime status changes
# WHY: Real-time monitoring dashboard showing live runtime status
# NOTE: Subscription not yet implemented in backend, placeholder for future
# subscription RuntimeStatusChanged($workspaceId: ID!) {
#   runtimeStatusChanged(workspaceId: $workspaceId) {
#     id
#     name
#     status
#     lastSeenAt
#   }
# }
