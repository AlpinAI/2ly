# Login Mutation
# WHY: Authenticate user with email/password, get tokens.
mutation Login($input: LoginInput!) {
  login(input: $input) {
    user {
      id
      email
    }
    accessToken
    refreshToken
    expiresIn
  }
}
