# Create MCP Registry Mutation
# WHY: Create a new private MCP registry for the workspace.
mutation CreateMCPRegistry($workspaceId: ID!, $name: String!) {
  createMCPRegistry(workspaceId: $workspaceId, name: $name) {
    id
    name
    createdAt
    lastSyncAt
  }
}
