# Create Skill Mutation
# WHY: Create a new skill for organizing tools
mutation CreateSkill($workspaceId: ID!, $name: String!, $description: String!) {
  createSkill(workspaceId: $workspaceId, name: $name, description: $description) {
    id
    name
    description
    createdAt
    updatedAt
  }
}
