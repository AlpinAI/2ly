# Get Skills Query
# WHY: Fetch all skills for a workspace
query GetSkills($workspaceId: ID!) {
  skills(workspaceId: $workspaceId) {
    id
    name
    description
    createdAt
    updatedAt
    mcpTools {
      id
      name
      description
      mcpServer {
        id
        name
      }
    }
  }
}
