# Get Toolset Key Query
# WHY: Fetch the identity key for a specific toolset to display in the Connect Toolset dialog
query GetToolsetKey($toolsetId: ID!) {
  toolsetKey(toolsetId: $toolsetId) {
    id
    key
    description
    createdAt
    expiresAt
    revokedAt
    relatedId
  }
}
