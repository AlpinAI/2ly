# Remove AI Provider Mutation
# WHY: Remove an AI provider configuration from the workspace
mutation RemoveAIProvider($workspaceId: ID!, $provider: AIProviderType!) {
  removeAIProvider(workspaceId: $workspaceId, provider: $provider)
}
