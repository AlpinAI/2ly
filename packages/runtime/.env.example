# Runtime Environment Configuration
# This file is a template - copy or let setup-local create it

# =============================================================================
# LOCAL DEVELOPMENT SETUP
# =============================================================================
# The runtime automatically loads environment variables in this order:
#   1. .docker-keys/.env.generated  (shared keys - lower priority)
#   2. packages/runtime/.env        (this file - higher priority, can override)
#
# Values auto-loaded from .docker-keys/.env.generated:
#   - SYSTEM_KEY (workspace-level access key)
#   - ENCRYPTION_KEY, NATS_OPERATOR_SEED
#   - JWT_PRIVATE_KEY_PATH, JWT_PUBLIC_KEY_PATH
#
# In development you only need to set values in this .env if you want to:
#   - Override shared values (e.g., use RUNTIME_KEY instead of SYSTEM_KEY)
#   - Add runtime-specific configuration (e.g., RUNTIME_NAME, ROOTS)

# =============================================================================
# Identity & Authentication (Choose ONE)
# =============================================================================
# System Key (system-level access) - Auto-loaded from .docker-keys/.env.generated
# Only uncomment to override with a different key type
# SYSTEM_KEY=WSK...your-system-key-here

# OR Runtime Key (runtime-specific access)
# RUNTIME_KEY=RTK...

# OR Skill Key (skill-specific access)
# SKILL_KEY=SKK...

# =============================================================================
# NATS Configuration
# =============================================================================
NATS_SERVERS=localhost:4222

# =============================================================================
# Runtime Configuration
# =============================================================================
RUNTIME_NAME=Main Runtime
ROOTS=TEMP:/tmp

# =============================================================================
# Agent Server Configuration (Optional)
# =============================================================================
# AGENT_SERVER_TRANSPORT=stream
# AGENT_SERVER_TRANSPORT_PORT=3001

# =============================================================================
# Logging
# =============================================================================
# Default log level for all loggers
LOG_LEVEL=info

# Pattern-based log level configuration (optional)
# Format: "logger=level,logger2=level2,pattern.*=level3"
# Examples:
#   LOG_LEVELS="mcp.*=debug"                    # Debug all MCP transport loggers
#   LOG_LEVELS="tool.*=trace"                   # Trace all tool operations
#   LOG_LEVELS="*=warn,skill=debug"             # Warn by default, debug skill
#   LOG_LEVELS="tool.server.*=silent"           # Silence tool server logs
#
# Available loggers: main, auth, health, nats, mcp.server, mcp.stdio, mcp.sse,
#                    mcp.streamable, fastify.manager, tool, tool.client, tool.server.*, skill
# LOG_LEVELS=

# =============================================================================
# MCP Server Configuration (Optional)
# =============================================================================
# MCP_CONFIG_PATH=/absolute/path/to/mcp.config.json
