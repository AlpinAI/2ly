# Backend Environment Configuration
# This file is a template - copy or let setup-local create it

# =============================================================================
# LOCAL DEVELOPMENT SETUP
# =============================================================================
# The backend automatically loads environment variables in this order:
#   1. .docker-keys/.env.generated  (shared keys - lower priority)
#   2. packages/backend/.env        (this file - higher priority, can override)
#
# Values auto-loaded from .docker-keys/.env.generated:
#   - ENCRYPTION_KEY, NATS_OPERATOR_SEED, SYSTEM_KEY
#   - JWT_PRIVATE_KEY_PATH, JWT_PUBLIC_KEY_PATH
#
# In development you only need to set values in this .env if you want to:
#   - Override shared values (e.g., different NATS server)
#   - Add backend-specific configuration (e.g., API keys)

# =============================================================================
# Cryptographic Keys (Auto-loaded from .docker-keys/.env.generated)
# =============================================================================
# These are automatically loaded from .docker-keys/.env.generated
# Only uncomment to override the shared values

# ENCRYPTION_KEY=your-64-char-encryption-key-here
# JWT_PRIVATE_KEY_PATH=/custom/path/to/private.pem
# JWT_PUBLIC_KEY_PATH=/custom/path/to/public.pem

# =============================================================================
# NATS Configuration
# =============================================================================
NATS_SERVERS=localhost:4222

# NATS operator seed (auto-loaded from .docker-keys/.env.generated)
# Only uncomment to override
# NATS_OPERATOR_SEED=SO...

# =============================================================================
# Database Configuration
# =============================================================================
DGRAPH_URL=localhost:8080

# =============================================================================
# Server Configuration
# =============================================================================
PORT=3000
NODE_ENV=development
CORS_ORIGINS=http://localhost:8888,http://localhost:3000

# =============================================================================
# JWT Configuration
# =============================================================================
JWT_ISSUER=skilder-platform
JWT_ACCESS_TOKEN_TTL=900        # 15 minutes
JWT_REFRESH_TOKEN_TTL=604800    # 7 days

# =============================================================================
# Logging
# =============================================================================
# Default log level for all loggers
LOG_LEVEL=info

# Pattern-based log level configuration (optional)
# Format: "logger=level,logger2=level2,pattern.*=level3"
# Examples:
#   LOG_LEVELS="dgraph=debug"                    # Debug DGraph operations
#   LOG_LEVELS="runtime.*=debug"                 # Debug all runtime loggers
#   LOG_LEVELS="*=warn,apollo=debug"             # Warn by default, debug Apollo
#   LOG_LEVELS="ai.provider.*=trace,nats=debug"  # Trace AI providers, debug NATS
#
# Available loggers: main, dgraph, nats, fastify, apollo, runtime, runtime.instance,
#                    identity, skill, monitoring, ai.provider.service, ai.provider.repository,
#                    key.rate.limiter, runtime.repository, identity.repository, skill.repository
# LOG_LEVELS=

# =============================================================================
# External APIs (Optional - for specific features)
# =============================================================================
# Azure AI Models
AZURE_ENDPOINT=https://models.inference.ai.azure.com
AZURE_API_KEY=your-azure-api-key

# Brave Search
BRAVE_SEARCH_API_KEY=your-brave-search-api-key
