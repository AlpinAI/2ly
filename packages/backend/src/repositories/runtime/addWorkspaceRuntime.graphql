mutation AddWorkspaceRuntime(
  $name: String!
  $description: String!
  $status: ActiveStatus!
  $type: RuntimeType!
  $createdAt: DateTime!
  $lastSeenAt: DateTime!
  $workspaceId: ID!
) {
  addRuntime(
    input: {
      name: $name
      description: $description
      status: $status
      type: $type
      createdAt: $createdAt
      lastSeenAt: $lastSeenAt
      workspace: { id: $workspaceId }
    }
  ) {
    runtime {
      id
      name
      description
      status
      type
      createdAt
      lastSeenAt
      workspace {
        id
        name
      }
      system {
        id
      }
    }
  }
}
