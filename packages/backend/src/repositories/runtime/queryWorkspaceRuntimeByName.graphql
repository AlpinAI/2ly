query QueryWorkspaceRuntimeByName($workspaceId: ID!, $name: String!) {
  getWorkspace(id: $workspaceId) {
    id
    runtimes(filter: { name: { eq: $name } }) {
      id
      name
      description
      status
      createdAt
      lastSeenAt
      processId
      hostIP
      hostname
      mcpClientName
      roots
    }
  }
}
