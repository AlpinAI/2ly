mutation AddMemberToWorkspace($userId: ID!, $workspaceId: ID!) {
  updateUser(input: { filter: { id: [$userId] }, set: { membersOfWorkspaces: { id: $workspaceId } } }) {
    user {
      id
      email
    }
  }
}
