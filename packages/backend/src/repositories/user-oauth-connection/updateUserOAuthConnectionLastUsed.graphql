mutation UpdateUserOAuthConnectionLastUsed($id: ID!, $now: DateTime!) {
  updateUserOAuthConnection(
    input: {
      filter: { id: [$id] }
      set: { lastUsedAt: $now }
    }
  ) {
    userOAuthConnection {
      id
      lastUsedAt
    }
  }
}
