query GetRegistryServer($id: ID!) {
  getMCPRegistryServer(id: $id) {
    id
    name
    description
    title
    repositoryUrl
    version
    packages
    remotes
    _meta
    createdAt
    lastSeenAt
    workspace {
      id
    }
    configurations {
      id
      name
    }
  }
}
